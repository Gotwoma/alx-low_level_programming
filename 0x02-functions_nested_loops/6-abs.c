#include "main.h"
/**
 * _abs - Returns absolute value of an integer
 * @i: value passed to find absolute value
 *
 * Return: absolute value
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
